mutation RegisterUser($input: RegistrationInputObject!) {
  registerUser(input: $input) {
    output {
      success
      message
      user {
        id
        username
      }
    }
    user {
      id
      email
      username
    }
  }
}
