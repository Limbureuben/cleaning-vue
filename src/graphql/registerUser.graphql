
  mutation REGISTER_USER($username: String!, $password: String!, $confirmPassword: String!, $role: String) {
    registerUser(username: $username, password: $password, confirmPassword: $confirmPassword, role: $role) {
      success
      message
      user {
        id
        username
        isStaff
        isSuperuser
      }
    }
  }

